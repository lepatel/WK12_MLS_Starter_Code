server {
    listen 3000;
    server_name _;

    root /usr/share/nginx/html;
    index index.html;

    # SPA routing: let react-router handle unknown paths client-side.
    location / {
        try_files $uri /index.html;
    }
}
